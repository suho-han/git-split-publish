# Legacy Compatibility Entry Point

This file is kept for backward compatibility with older installations that
reference `CLAUDE.md` directly.

Use `SKILL.md` as the canonical skill entry and follow
`references/workflow.md` plus `references/grouping-rules.md`.
